{% extends 'layout.html.twig' %}

{% block title %}Edit Car{% endblock %}
{% block page_breadcrumb %}{{ parent() }} <span> / edit car</span>{% endblock %}
{% block body %}
    <h1>Edit Car</h1>

    {{ include('car/_form.html.twig', {'button_label': 'Update'}) }}

    <div class="btn-group-operations form-group">
        <a href="{{ path('car_index') }}" class="btn btn-default">back to list</a>

        {{ include('car/_delete_form.html.twig') }}
    </div>
{% endblock %}
